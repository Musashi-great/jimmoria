# Ticker Collision Review

Use this when a project name or ticker is short, generic, or reused by multiple projects.

- Search exact ticker plus chain, contract, project name, and official links.
- Keep candidates separate until official sources resolve identity.
- Mark unresolved collisions as unverified.
- Do not merge social mentions across different projects that share a ticker.
